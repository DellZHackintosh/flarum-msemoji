flarum-msemoji:

  ##
  # UNIQUE KEYS - The following keys are used in only one location each.
  ##

  # Translations in this namespace are used by the forum user interface.
  forum:

    # These translations are used by the composer (emoji autocompletion function).
    composer:
      emoji_tooltip: Insert emoji
      type_to_search_text: Type to search for an emoji

  admin:

    settings:

      base: Base
      folder: Folder
      ext: Extension
      basehelp: "Tips: Type $ will replace with automatic version number. Case sensitive."
      baseph: 'Example: https://www.example.com/'
      folderph: 'Example: svg'
      extph: 'Example: .gif'
      disable_autocomplete: Disable Auto Complete
